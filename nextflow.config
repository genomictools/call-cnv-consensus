
singularity {
  enabled = true
  autoMounts = true
}

params {
  // Containers
  bcftools      = 'bcftools.1.19'
  samtools      = 'samtools.1.9'
  bioconductor  = 'mahshaaban/bioconductor:3.19'
  rocker        = 'rocker/tidyverse:latest'
  rvs           = 'rvs.0.1'
  famagg = 'bioconductor-famagg-1.30.0'
  
  // Default params
  "gtc"     : "$TOY_DATASETS/arrays/gtc/*.gtc.txt",
  "dbsnp"   : "$TOY_DATASETS/arrays/*.vcf.gz{,.tbi}",
  "hmm"     : "$TOY_DATASETS/arrays/hhall_0.hmm",
  "gc"      : "$TOY_DATASETS/arrays/hg38.gc5Base.short.txt",
  "snplist" : "$TOY_DATASETS/arrays/snp_list.txt",
  "refgene" : "$TOY_DATASETS/arrays/refGene.txt",
  "reflink" : "$TOY_DATASETS/arrays/refLink.txt"
}

profiles {
  // Resource profiles
  local     { includeConfig 'conf/local.config' }
  cluster   { includeConfig 'conf/cluster.config' }
  dynamic   { includeConfig 'conf/dynamic.config' }
  
  // Test profile (Same as default params)
  test {
    params {
      variants  = "$TOY_DATASETS/wes-sarek/*.variants.vcf.gz{,.tbi}"
      pedigrees = "$TOY_DATASETS/wes-sarek/*.families.ped"
    }
  }

  // Test on gh-action
  gha {
    params {
      variants  = "input/*.variants.vcf.gz{,.tbi}"
      pedigrees = "input/*.families.ped"
    }
  }
}
